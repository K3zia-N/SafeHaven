import { SafeLocationsFinder } from './safe-locations-finder';

export default function SafeLocationsPage() {
    return (
        <div className="max-w-4xl mx-auto animate-in fade-in-50">
            <SafeLocationsFinder />
        </div>
    );
}
