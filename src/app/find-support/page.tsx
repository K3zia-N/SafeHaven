import { FindSupportTool } from './find-support-tool';

export default function FindSupportPage() {
    return (
        <div className="max-w-4xl mx-auto animate-in fade-in-50">
            <FindSupportTool />
        </div>
    );
}
